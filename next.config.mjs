/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    serverActions: {
      allowedOrigins: ["qb8bl3jg-3000.euw.devtunnels.ms/"],
    },
  },
}

export default nextConfig
